# Images

Drop real portfolio photographs and the artist portrait here, then point the
components at them (see the root `README.md` → "Replacing placeholders").

Suggested files:

- `artist.jpg` — portrait of Telma Fattori (used in `components/About.tsx`)
- `portfolio-01.jpg` … `portfolio-08.jpg` — the eight portfolio works
- `og.jpg` — 1200×630 social-share image (used in `app/layout.tsx`)

Until these exist, the site renders elegant placeholders from placehold.co.
